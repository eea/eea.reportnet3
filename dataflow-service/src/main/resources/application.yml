server:
  port: 8020
spring:
  application:
    name: dataflow
eea:
  security:
    authenticatedRequest: /dataflow/pendingaccepted
    roleProtectedRequest:
      DATA_CUSTODIAN: /dataflow/createDataFlow