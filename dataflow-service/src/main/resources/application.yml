server:
  port: 8020
spring:
  application:
    name: dataflow
eea:
 security:
   authenticatedRequest: /dataflow/pendingaccepted,/dataflow/completed,/dataflow/request/*
   roleProtectedRequest:
     DATA_CUSTODIAN: /dataflow/createDataFlow