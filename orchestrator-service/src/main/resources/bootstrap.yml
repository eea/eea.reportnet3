spring:
  cloud:
    consul:
      discovery:
        preferIpAddress: true
        instanceId: ${spring.application.name}:8091
        deregister: true
      config:
        name: orchestrator
      host: ${CONSUL_HOST:localhost}
      port: ${CONSUL_PORT:8500}